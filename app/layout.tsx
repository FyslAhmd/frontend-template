import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Modern Frontend Template | Stunning UI",
  description: "A state-of-the-art Next.js frontend template with premium aesthetics, rich animations, and excellent performance.",
  keywords: ["Next.js", "React", "Frontend", "Template", "UI", "Design"],
  openGraph: {
    title: "Modern Frontend Template",
    description: "A stunning, responsive frontend template built with Next.js and Tailwind CSS.",
    type: "website",
  }
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
